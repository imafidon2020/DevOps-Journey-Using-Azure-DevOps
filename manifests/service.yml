apiVersion: v1
kind: Service
metadata:
    name: imafidondevopsjourneyusingazuredevops
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: imafidondevopsjourneyusingazuredevops